#/bin/sh
# Test whether added scala files correctly formatted.

for sc in $( git diff --cached --name-only | grep '.scala$' )
do
  git show :$sc | ./reform --stdin -t
  if [ $? -ne 0 ]; then
    echo "'$sc' is badly formed. run 'reform-added'."
    exit 1
  fi
done
